npm / package.json

ES6-syntaks
 - default parameterverdi
 - () => {}
 - spread
 - Object.assign  Object.assign(state, { visibilityFilter: action.filter }) is also wrong: it will mutate the first argument. You must supply an empty object as the first parameter.
 - let

SCSS

Google Chrome-verktøyene
+ React-plugin'en og $r.store.getState()
+ Redux-pluginen

React komponent-livssyklus

Installere dependencies med npm

De forskjellige måtene en komponent kan få input på (props, omponent-state, redux-state, interne veriabler, context)

Reducere skal bare returnere nye objekter når de er endra. Implikasjoner. Equals-sjekk.
